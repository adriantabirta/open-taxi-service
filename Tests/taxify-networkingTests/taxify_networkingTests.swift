import XCTest
@testable import taxify_networking

final class taxify_networkingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(taxify_networking().text, "Hello, World!")
    }
}
